import { DogWeightPipe } from './dog-weight-pipe';

describe('DogWeightPipe', () => {
  it('create an instance', () => {
    const pipe = new DogWeightPipe();
    expect(pipe).toBeTruthy();
  });
});
